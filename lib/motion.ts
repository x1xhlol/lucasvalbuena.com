// Framer-motion mirror of the CSS motion tokens in app/globals.css (--ease-out).
// Keep both in sync so JS-driven and CSS-driven animations share one curve.
export const EASE_OUT = [0.23, 1, 0.32, 1] as const
